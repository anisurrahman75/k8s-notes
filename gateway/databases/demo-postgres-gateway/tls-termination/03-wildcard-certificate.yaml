apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pg-wildcard-cert
  namespace: demo-db
spec:
  secretName: pg-wildcard-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - "*.db.infrasnow.com"
  renewBefore: 720h
