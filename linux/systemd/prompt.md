I want to lean Systemd + systemd-run ( main focus) and how to use it effectively. 

What I want to learn:
- what is systemd, it's use case
- What is systemd-run, use case, and how to use it effectively
- Inside this current folder, create a demo golang project the burn memory + cpu. 
- Now, run this binary in systemd + systemd-run 
- Now, limit CPU + Resources, Evern more customization ,like isolation with cgroups, etc.
- Also Run a this golang binary by bwrap by complete isolation, so that this process can't read other any files. ( a complete isolation with bwrap) 
- The more concepts of bwrap


## How you organize each stuff

- Create a concept directory, there keep each concept in a separate markdown file, like systemd.md, systemd-run.md, bwrap.md, etc.
- Create a demo directory, there keep the demo golang project and any scripts or configuration files
- And a quiz sections. 
- Overall keep everything as short notes, not so long, but enough to understand the concept and use it effectively.
- Overall use your creative way to organize the content, but keep it simple and easy to understand.
- Thinks like human notes, not AI notes.
